parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
            - '../src/DBAL/'
            - '../src/OpenApi/' # Wieder hinzugefügt: OpenApi-Komponente ausschließen

    # OpenAPI-spezifische Service-Wiring bleibt deaktiviert.
    # /api/calendar wird über einen manuellen Controller in src/Controller bereitgestellt.

    # Keycloak/JWT Authentication Services
    App\Security\JwtAuthenticator:
        arguments:
            $oidcIssuer: '%env(OIDC_ISSUER)%'
            $entityManager: '@doctrine.orm.entity_manager'
            $provisioningService: '@App\Service\KeycloakUserProvisioningService'
            $logger: '@logger'

    App\Service\KeycloakUserProvisioningService:
        arguments:
            $entityManager: '@doctrine.orm.entity_manager'
            $logger: '@logger'

