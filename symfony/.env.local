# App Settings
DEFAULT_URI=http://localhost:8000

# Database connection - SQLite für lokale Tests (keine externe DB nötig)
DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"

# Mailer
MAILER_DSN=null://null

